import React, { useState } from 'react';

export default function PassW=()=>{ 
    const [text, setText] = useState('');

    return (
        <div className="App">
        <input type="text" placeholder="Type here..." onChange={(e) => setText(e.target.value)} />
        <button onClick={() =>set
            
        </div>
    );